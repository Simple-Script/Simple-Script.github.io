text(There is not much syntax because you can config the code in any way.)
text(You can put it all in one line without hitting return or you could hit return after every code line.)
text(just choose yourself.)
